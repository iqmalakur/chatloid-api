# Do not edit this file directly.
# Use docker-compose.override.yml to override values (environment, ports, etc.)

services:
  db:
    image: postgres:17
    ports:
      - "5432:5432"
    environment:
      - POSTGRES_USER=
      - POSTGRES_PASSWORD=
      - POSTGRES_DB=
    volumes:
      - chatloid-data:/var/lib/postgresql/data

volumes:
  chatloid-data:
